.Net build
    - To build the project: dotnet run --project .\services\greetings\greetings.csproj 

Docker:
    - To build the image and start the container: docker-compose up -d --build
    - docker images
    - docker ps -a

Scenarios
    - An image hosted in single container (DONE)
    - Multiple containers with the same image behind load balancer
    - UI with service layer
    - Multi Container -> Communication between them
    - Application Gateway
    - Common Login service